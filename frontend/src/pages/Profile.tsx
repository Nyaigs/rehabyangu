import { useAuth } from '../context/AuthContext';
export default function Profile() { const { user } = useAuth(); return <div className="mx-auto max-w-2xl space-y-6"><div><h1 className="text-page-title text-secondary-800">My profile</h1><p className="text-body text-secondary-500">Your signed-in account details.</p></div><section className="card space-y-4 p-6"><div><p className="form-label">Username</p><p className="text-body">{user?.username || '—'}</p></div><div><p className="form-label">Email</p><p className="text-body">{user?.email || '—'}</p></div><p className="text-body text-secondary-500">For account or password changes, contact your facility administrator.</p></section></div>; }
