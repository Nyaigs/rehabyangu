import { useQuery } from '@tanstack/react-query'; import { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip } from 'recharts'; import api from '../api/client'; import { SkeletonCard } from '../components/Skeleton';
export default function Reports() { const report = useQuery({ queryKey: ['dashboard-stats'], queryFn: async () => (await api.get('/dashboard/stats/')).data }); const data = [{ name: 'Patients', value: report.data?.total_patients || 0 }, { name: 'Active', value: report.data?.active_patients || 0 }, { name: 'Appointments', value: report.data?.appointments_today || 0 }]; return <div className="space-y-6"><div><h1 className="text-page-title text-secondary-800">Reports & analytics</h1><p className="text-body text-secondary-500">Current tenant patient, operational, and revenue summary.</p></div>{report.isLoading ? <SkeletonCard count={3} /> : <><section className="grid gap-4 md:grid-cols-3"><Metric label="Patients" value={report.data?.total_patients || 0} /><Metric label="Active admissions" value={report.data?.active_patients || 0} /><Metric label="Outstanding balance" value={`KES ${Number(report.data?.outstanding_invoice_value || 0).toLocaleString()}`} /></section><section className="card p-6"><h2 className="text-section-title">Care activity</h2><div className="mt-4 h-72" aria-label="Care activity bar chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><XAxis dataKey="name" /><YAxis /><Tooltip /><Bar dataKey="value" fill="#17614f" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></section></>}</div>; }
function Metric({ label, value }: { label: string; value: string | number }) { return <article className="card p-5"><p className="text-body text-secondary-500">{label}</p><p className="mt-2 text-page-title text-secondary-800">{value}</p></article>; }
