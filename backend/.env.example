SECRET_KEY=replace-with-a-unique-django-secret
DEBUG=False
ALLOWED_HOSTS=api.example.com
CORS_ALLOWED_ORIGINS=https://app.example.com
# Generate once with Python: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
FIELD_ENCRYPTION_KEY=
# M-Pesa service configuration (sandbox by default; do not use production credentials in development)
MPESA_ENVIRONMENT=sandbox
PLATFORM_MPESA_SHORTCODE=
PLATFORM_MPESA_SHORTCODE_TYPE=paybill
PLATFORM_MPESA_CONSUMER_KEY=
PLATFORM_MPESA_CONSUMER_SECRET=
PLATFORM_MPESA_PASSKEY=
